global:
  environment: dev

db:
  auth:
    password: "dev-password-123"
    postgresPassword: "dev-admin-123"
  image:
    tag: "15.6.0-debian-11-r16"

backend:
  image:
    repository: suryaputraku/spring-boot-crud
    tag: latest
    pullPolicy: IfNotPresent
    
frontend:
  replicaCount: 1
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 128Mi

ingress:
  host: dev.parcial1.ejemplo.com
